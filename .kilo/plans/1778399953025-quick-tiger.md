# Plan: README Audit for AI-Agent Discoverability

## Findings

### 1. Missing `about` command in CLI System table (line 73-78)

The `--help` output includes `about [options]` under "System" (added in commit 36033b3), but the README table only lists `mcp`. This breaks parity with the actual CLI and makes the troubleshooting command invisible to agents parsing the README.

**Fix:** Add a row for `about` to the System table:
```
| `about [options]` | Show version and environment info for troubleshooting |
```

### 2. No other issues found

- **Version pinning:** `scripts/update-version.js` auto-pins all `xno-skills@X.Y.Z` occurrences in README.md. Currently shows `3.1.2` everywhere — correct.
- **OpenCode issue link (line 85):** [#15535](https://github.com/anomalyco/opencode/issues/15535) is still open. The parenthetical is accurate and useful context.
- **Similar Projects links:** Both repos exist and are active (kilkelly/nano-currency-mcp-server, strawberry-labs/berrypay-cli).
- **Referenced files:** `LIBRARY.md`, `RELEASING.md`, `assets/xno-skills-cli.png` all exist.
- **OWS links:** `https://openwallet.sh/#quickstart` and `npx skills add open-wallet-standard/core@ows` are correct.
- **Skill install command:** `npx skills add -g CasualSecurityInc/xno-skills` matches the actual working syntax.
- **MCP config examples:** Cover Codex, Claude Desktop, OpenCode, Gemini CLI, Antigravity, VS Code — all use the correct `npx -y -p xno-skills@3.1.2 xno-mcp` pattern.
- **Security notes:** Still relevant and well-structured.
- **Migration notice (line 22):** "before May 4, 2026" — recent and useful. Will age out naturally.

## Changes

Single edit to `README.md`:

**File:** `README.md`, line 73-78 (System table)

Before:
```markdown
### System

| Command | Description |
|---|---|
| `mcp` | Start the MCP server or view configuration instructions |
```

After:
```markdown
### System

| Command | Description |
|---|---|
| `about [options]` | Show version and environment info for troubleshooting |
| `mcp` | Start the MCP server or view configuration instructions |
```

No version bump needed — this is a docs-only change that doesn't affect the published package.
